# GeoResilience Geospatial Engine

Responsible for connectivity and infrastructure impact analysis.

## Workflow
1. Store raw geospatial datasets in `data/`
2. Run connectivity scripts in `src/`
3. Export impact analysis to `outputs/`

## Integration
Outputs must match `shared/contracts/infrastructure-impact.json`.
